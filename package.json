{
  "name": "api.jordanadams.co.uk",
  "private": true,
  "scripts": {
    "lint": "snazzy src",
    "watch": "nodemon --exec \"babel-node src/server.js -i node_modules\"",
    "clean": "rm -rf dist node_modules",
    "build": "babel src -d dist",
    "clean-build": "npm run clean && npm run build",
    "start": "node dist/server.js"
  },
  "author": "Jordan Adams <node_modules>",
  "dependencies": {
    "dotenv": "^2.0.0",
    "hapi": "^13.0.0",
    "hapi-cors-headers": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-watch": "^1.0.2",
    "nodemon": "^1.9.1",
    "snazzy": "^3.0.0",
    "standard": "^6.0.7"
  }
}
